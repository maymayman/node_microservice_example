const express = require('express');
const router = express.Router();

/* GET users listing. */
// router.get('/', function(req, res, next) {
//   const err = new createError.Unauthorized('Login require !')
//   return next(err)
//   res.send('respond with a resource');
// });

module.exports = router;
